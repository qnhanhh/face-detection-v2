const Navigation = () => {
  return (
    <nav className="absolute right-2 p-4 hover:cursor-pointer">
      <p className="text-lg underline hover:text-slate-500">Sign Out</p>
    </nav>
  );
};

export default Navigation;
